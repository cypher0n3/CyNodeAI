# Sandbox Agent instruction bundle

This directory is the canonical baseline for the Sandbox Agent (SBA). It can be supplied in the job (context.baseline_context) or baked into the SBA image. Same format as PMA bundles; load with the same loader (e.g. LoadInstructions). See docs/tech_specs/cynode_sba.md.
